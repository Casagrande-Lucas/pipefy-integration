services:
  postgres:
    image: postgres:17-alpine
    container_name: pipefy_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-pipefy_integration}
      POSTGRES_USER: ${POSTGRES_USER:-pipefy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pipefy}
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pipefy} -d ${POSTGRES_DB:-pipefy_integration}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pipefy_api
    ports:
      - "${API_PORT:-8080}:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    environment:
      # Overrides database.host so the API reaches postgres by service name.
      # All other values come from the mounted config.yaml.
      PIPEFY_DATABASE_HOST: postgres
      PIPEFY_DATABASE_PORT: "5432"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: pipefy_pgadmin
    profiles:
      - pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@admin.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
